<% if current_user.has_role? :trainer %>
<h3>List of programmes (Draft)</h3>
  <%= link_to 'New programme', new_programme_path, class:'btn btn-success' %>
  <br>
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Description</th>
        <th>Due date</th>
        <th>State</th>
      </tr>
    </thead>
    <tbody>
      <% @programmes.reverse_order.each do |programme| %>
        <% if (programme.user == current_user) && (programme.state == 'draft')%>
          <tr>
            <td><%= link_to programme.description, programme %></td>
            <td><%= programme.due_date %></td>
            <td><%= programme.state %></td>
          </tr>
        <% end %>
      <% end %>
    </tbody>
  </table>
<% end %>


  <h3>List of programmes (Assigned)</h3>
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Description</th>
        <th>Due date</th>
        <th>State</th>
      </tr>
    </thead>
    <tbody>
      <% @programmes.reverse_order.each do |programme| %>
        <% if (programme.user == current_user)%>
          <tr>
            <td><%= link_to programme.description, programme %></td>
            <td><%= programme.due_date %></td>
            <td><%= programme.state %></td>
          </tr>
        <% end %>
      <% end %>
    </tbody>
  </table>

<% if current_user.has_role? :trainer %>
  <h3>List of programmes (Completed)</h3>
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Description</th>
        <th>Due date</th>
        <th>State</th>
      </tr>
    </thead>
    <tbody>
      <% @programmes.reverse_order.each do |programme| %>
        <% if (programme.user == current_user) && (programme.state == 'completed')%>
          <tr>
            <td><%= link_to programme.description, programme %></td>
            <td><%= programme.due_date %></td>
            <td><%= programme.state %></td>
          </tr>
        <% end %>
      <% end %>
    </tbody>
  </table>
<% end %>